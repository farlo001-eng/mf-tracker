2026-07-06  Owner tracking: owners table linkable to properties
            - db.js: new `owners` table; `properties.owner_id` (FK, set null on delete);
              `touches.owner_id` (FK, cascade) + `touches.property_id` made nullable +
              touches_one_target CHECK (exactly one of property_id/owner_id); guarded
              one-time migrateOwnersFromPropertyNames() backfill runs in ensureSchema()
            - server.js: full owners CRUD (GET list w/ rollups, POST, GET/PATCH/DELETE :id,
              POST :id/touch, POST /merge); GET /api/properties + /:id join owners and
              return linked_owner_name; PATCH /api/properties/:id allows owner_id; POST
              /api/import links/creates an owner per row, returns ownersCreated
            - App.jsx: top-level Desk/Properties/Owners nav in Tracker; DeskView unifies
              due owners+properties; PropertyRow no longer owns modal state — PropertyModal
              extracted so any view can open it by id; new OwnerLink combobox (search/link/
              create/unlink) in the property modal; new owner_id "Linked owner" column in
              the properties table; OwnersView/OwnerRow/AddOwnerForm/OwnerModal added,
              mirroring the properties table/detail patterns
            - Manual step still required: run pg_dump backup against production
              DATABASE_URL before deploying (no DB credentials available in the dev
              sandbox that built this change)
2026-06-08  File attachments stored in Postgres (Excel/PDF, 10 MB cap)
            - db.js: attachments table (id, property_id, filename, mime, size, data bytea,
              created_at); idx_attach_prop index on property_id
            - server.js: JSON body limit raised 8mb → 20mb; POST /api/properties/:id/attachments
              (base64 decode, size check, insert); GET /api/attachments/:id (stream file);
              DELETE /api/attachments/:id; GET /api/properties/:id now also queries
              attachments and returns attachments[] array alongside touches[]
            - App.jsx: Paperclip + FileText icons added to lucide-react import; fmtSize()
              helper (KB/MB display); Attachments component (FileReader base64 upload,
              per-item delete, Add file button); rendered in detail modal just before
              "All property data" section
2026-06-05  Slate theme + editable/deletable touch log entries
            - styles.css: replace warm parchment palette with slate blue-grey neutrals
              (--rust → steel blue #41709c, --paper → #e9edf0, --surface → #fff, etc.);
              overlay tints cooled to rgba(20,28,40,...)
            - server.js: PATCH /api/touches/:id (update date/channel/note) and
              DELETE /api/touches/:id handlers added after POST touch handler
            - App.jsx: reload() helper in PropertyRow; touches list uses TouchRow
              component; TouchRow shows view mode with Pencil/Trash2 buttons and
              inline edit form (channel select + date input + textarea + Save/Cancel)
2026-06-04  Group "All property data" into labelled sections
            - App.jsx: FIELD_GROUPS constant (7 sections: Property, Location, Rent & unit
              mix, Owner as listed, True owner, Sale history, Financing); groupedExtra()
              builds groups from d.extra + appends Additional for uncategorised keys;
              flat orderedExtraKeys grid replaced with grouped grid — section titles span
              full width in mono/uppercase/rust with lineSoft bottom border
2026-06-04  Fix rent table: prefill type labels and styled bordered layout
            - App.jsx: RENT_SEED gains label column; seedRent destructures [label,u,s,r]
              and sets type:label so Studio/1BR/2BR/3BR/4BR auto-fill from imported data;
              table wrapped in <div className="rent-wrap">; delete <td> gets .rent-del
            - styles.css: .rent-wrap (bordered rounded container); th padding+bg+border;
              td grid borders (no bottom on last row, no right on last col); inputs
              borderless with focus outline; .rent-psf gets surface2 bg; .rent-del added
2026-06-04  Popup overhaul: rent table, header links, ordered extra fields, AddField
            - db.js: extra_order jsonb + rent_table jsonb columns (idempotent alters)
            - server.js: GET returns extra_order; import stores order; PATCH handles
              orderAppend (jsonb concat) and rent_table replacement
            - styles.css: .head-link, .rent-tbl/th/td/input/.num, .rent-psf rules
            - App.jsx: Globe/Pencil/Tag/Calendar icons; mapsUrl/siteUrl/RENT_SEED/
              seedRent/psf/orderedExtraKeys helpers; WebsiteLink/RentTable/AddField
              components; Maps link + WebsiteLink in popup header; key-stats row
              (units/rent type/year built) above owner block; RentTable after owner;
              "All property data" shows all keys (blank too) in import order with AddField;
              extraKeys useMemo now order-aware; doImport sends order:headers
2026-06-04  Add per-column filter row (Excel-style)
            - App.jsx: Filter icon; showFilters/filters/setFilter state; activeFilters/filterCount/
              filtered (useMemo); Filters button + Clear link in toolbar; tbl-filter <tr> in <thead>;
              <tbody> now renders filtered list with "no match" empty state
            - styles.css: .tbl-filter th/input/select rules appended
2026-06-04  Add drag-to-reorder for table columns
            - App.jsx: useRef added; dragCol ref + moveCol splice helper in Tracker;
              <th> elements get draggable + onDragStart/onDragOver/onDrop handlers
            - styles.css: th[draggable]:active sets cursor:grabbing + rust color
2026-06-04  Replace inline table-row detail with portal modal overlay
            - styles.css: .detail-modal / .detail-head / .detail-body CSS appended
            - App.jsx: createPortal import; Escape-to-close handler; inline tbl-detail tr
              replaced with createPortal(modal-bg > card.detail-modal, document.body);
              loading state shown while detail fetches; Delete now calls onToggle before onChange
2026-06-04  Notes field, column picker, date fix, scrollable table
            - db.js: add notes text column migration (idempotent)
            - server.js: to_char date formatting for next_follow_up in list + detail; notes in PATCH allowed list
            - styles.css: tbl-wrap gets max-height:68vh + overflow:auto; column picker .colbtn/.colpop CSS
            - App.jsx: useMemo import; Columns icon; CORE_COLS/DEFAULT_COLS/colDesc replaces TABLE_COLS;
              column visibility picker with localStorage; extraKeys/allKeys/visibleCols in Tracker;
              PropertyRow accepts cols prop; NotesBox component; "All property data" values now inline-editable
2026-06-03  Replace card list with inline-editable spreadsheet table
            - styles.css: appended .tbl-wrap / .tbl / .cell-* / .tbl-detail CSS classes
            - server.js: GET /api/properties returns p.extra; PATCH merges extra via jsonb ||
            - App.jsx: TABLE_COLS + colVal/colPatch helpers; maxWidth 780→1180; list → table;
              PropertyRow rewritten as <tr>/<tr> pair; CellEdit component added
2026-06-03  Add per-property `extra` JSONB field; surface full CSV row in detail view
            - db.js: idempotent ALTER TABLE migration in SCHEMA
            - server.js: slim list SELECT (no p.*); import stores & upserts extra
            - App.jsx: payload includes raw row as extra; detail shows "All property data" disclosure
