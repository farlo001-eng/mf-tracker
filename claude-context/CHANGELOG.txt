2026-06-04  Add drag-to-reorder for table columns
            - App.jsx: useRef added; dragCol ref + moveCol splice helper in Tracker;
              <th> elements get draggable + onDragStart/onDragOver/onDrop handlers
            - styles.css: th[draggable]:active sets cursor:grabbing + rust color
2026-06-04  Replace inline table-row detail with portal modal overlay
            - styles.css: .detail-modal / .detail-head / .detail-body CSS appended
            - App.jsx: createPortal import; Escape-to-close handler; inline tbl-detail tr
              replaced with createPortal(modal-bg > card.detail-modal, document.body);
              loading state shown while detail fetches; Delete now calls onToggle before onChange
2026-06-04  Notes field, column picker, date fix, scrollable table
            - db.js: add notes text column migration (idempotent)
            - server.js: to_char date formatting for next_follow_up in list + detail; notes in PATCH allowed list
            - styles.css: tbl-wrap gets max-height:68vh + overflow:auto; column picker .colbtn/.colpop CSS
            - App.jsx: useMemo import; Columns icon; CORE_COLS/DEFAULT_COLS/colDesc replaces TABLE_COLS;
              column visibility picker with localStorage; extraKeys/allKeys/visibleCols in Tracker;
              PropertyRow accepts cols prop; NotesBox component; "All property data" values now inline-editable
2026-06-03  Replace card list with inline-editable spreadsheet table
            - styles.css: appended .tbl-wrap / .tbl / .cell-* / .tbl-detail CSS classes
            - server.js: GET /api/properties returns p.extra; PATCH merges extra via jsonb ||
            - App.jsx: TABLE_COLS + colVal/colPatch helpers; maxWidth 780→1180; list → table;
              PropertyRow rewritten as <tr>/<tr> pair; CellEdit component added
2026-06-03  Add per-property `extra` JSONB field; surface full CSV row in detail view
            - db.js: idempotent ALTER TABLE migration in SCHEMA
            - server.js: slim list SELECT (no p.*); import stores & upserts extra
            - App.jsx: payload includes raw row as extra; detail shows "All property data" disclosure
